# Color sintax for Vachilang

> VS Code

Installation requires NodeJS/BunJS

- Install VSCE
  ```bash
  bun/npm i -g vsce
  ```
- Compile
  > Yes to all warnings
  ```bash
  vsce package
  ```
- Install
  ```bash
  code --install-extension vachilang-syntax-highlighting-0.0.1.vsix
  ```
